{description}
    Aliases: {commands}
    Default Options: {defaultoptions}
    Usage:
      1. {command}
      2. {command} <partialname | entityid | steamid>
    Notes:
      Use bc-help to get a list of common /options
      This is an alias command for bc-lp

      Filter options:
      /n - player name
      /s - player steamId

    'bc-id /n' would present a list of players names and entityIds
