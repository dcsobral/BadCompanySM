namespace BCM.Commands
{
  public class BCBlocks : BCGameObjects
  {
    public override void Process()
    {
      //if (Options.ContainsKey("filter"))
      //{
      //  SendOutput("Error: Can't set filters on this alias command");
      //  SendOutput(GetHelp());

      //  return;
      //}

      //Options.Add("filter", BCMItemClass.StrFilters.Type + "," + BCMItemClass.StrFilters.Name);
      //Params.Insert(0, "blocks");
      //var cmd = new BCGameObjects();
      //cmd.Process(Options, Params);
    }
  }
}
